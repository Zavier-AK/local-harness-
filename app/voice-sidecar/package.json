{
  "name": "harness-voice-sidecar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Voice helpers for the harness: Laya (laya-server.mjs) and the voice browser (browser-server.mjs). Each is spoken to over stdin/stdout, one JSON object per line.",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@receptron/laya": "^0.1.2",
    "playwright-core": "^1.63.0"
  }
}
