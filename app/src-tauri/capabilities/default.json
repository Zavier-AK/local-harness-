{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Folder picking for choosing a project directory, plus the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
